# AI Trading Agent Environment Configuration

# OpenAI API Configuration
#### OpenAI / Azure OpenAI configuration
# Standard OpenAI (api.openai.com)
# - Keep your secret key safe. Do NOT commit secrets to source control.
OPENAI_API_KEY=your_openai_api_key_here
# Default model used by the agent run loops (can be overridden per-run)
OPENAI_MODEL=gpt-4  # or gpt-3.5-turbo for cost savings
# If you have access to GPT-5 via your OpenAI plan or provider, set here:
# OPENAI_MODEL=gpt-5

# Optional settings (tweak as needed)
# request timeout (seconds)
OPENAI_TIMEOUT=30
# default temperature for runs (0.0-1.0)
OPENAI_TEMPERATURE=0.3
# max tokens for completions if applicable
OPENAI_MAX_TOKENS=1500

# If you are using Azure OpenAI, set these instead of (or in addition to) the
# plain OPENAI_API_KEY above. Example:
# OPENAI_API_TYPE=azure
# OPENAI_API_BASE=https://my-openai-resource.openai.azure.com/
# OPENAI_API_VERSION=2024-10-01-preview
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4-deployment
OPENAI_API_TYPE=openai        # set to 'azure' when using Azure OpenAI
OPENAI_API_BASE=              # e.g. https://<your-azure-resource>.openai.azure.com/
OPENAI_API_VERSION=2024-10-01-preview
AZURE_OPENAI_DEPLOYMENT_NAME= # (required for Azure when making model calls)

# Optional organization header (OpenAI teams/org):
OPENAI_ORGANIZATION=

# Retry behavior
OPENAI_RETRIES=2

# Responses API / Background mode (if used by backend)
# When using the Responses API with background mode, you can opt to store
# the background responses server-side for later polling. Example boolean:
OPENAI_RESPONSES_STORE=true


# News API for sentiment analysis
NEWS_API_KEY=your_news_api_key_here

# Alpha Vantage for additional market data (optional)
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here

# Debug and Development
DEBUG_PROMPTS=false  # Set to true to log GPT prompts
AGENT_CACHE_TTL=300  # Cache TTL in seconds (default 5 minutes)

# Trading Configuration
DEFAULT_TIMEFRAME=1d
MAX_CONCURRENT_ANALYSES=5
ENABLE_SENTIMENT_ANALYSIS=true

# Risk Management
DEFAULT_RISK_LEVEL=MEDIUM
ENABLE_STOP_LOSS=true
DEFAULT_STOP_LOSS_MULTIPLIER=2.0
DEFAULT_TARGET_MULTIPLIER=3.0
